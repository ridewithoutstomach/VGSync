# KVRouite - packages needed to RUN the application.
#
# Windows/macOS:  pip install -r requirements.txt
# Linux:          GStreamer comes from the distribution, not from pip - see
#                 README.md. Create the venv with --system-site-packages.

fitparse==1.2.0
gstreamer-bundle==1.28.6; sys_platform != "linux"
pillow==12.3.0
PySide6==6.11.2
PySide6_Addons==6.11.2
PySide6_Essentials==6.11.2
shiboken6==6.11.2
