# KVRouite - additional packages for BUILDING the Windows executable.
# Install on top of requirements.txt:
#
#     pip install -r requirements.txt
#     pip install -r requirements-build.txt
#     python build_with_pyinstaller.py
#
# Nothing in here is imported by the application itself.
#
# All versions are the current releases. Verified by actually building 5.0 with
# them: PyInstaller run, KVRouite.exe and the Inno Setup installer produced and
# started.
altgraph==0.17.5
packaging==26.3
pefile==2024.8.26
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
pywin32-ctypes==0.2.3
setuptools==84.0.0
