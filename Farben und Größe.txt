

Farbe Marker Pause 
def on_user_selected_index(self, new_index: int):

blau:
self.map_widget._color_point(new_index, "#0000FF")
gelb:
        #self.map_widget._color_point(new_index, "#FFFF00")
        """


Größe Marker Pause:
mao_widget.py:
 js = f"highlightPoint({index}, '{color}', 6, false);"

def _color_point(self, index: int, color: str):
        """
        Ruft highlightPoint(...) in JS auf,
        damit der Punkt index = 'color' bekommt.
        """
        # highlightPoint(index, color, size=4, center=false):
        # Größe Marker festlegen
        js = f"highlightPoint({index}, '{color}', 6, false);"
        self.view.page().runJavaScript(js)

Neu:
Größe und Farbe :
in mainwindow.py:
 self.map_widget._color_point(new_index, "#0000FF", size=6)
